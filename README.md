# Требования
python>=3.10

poetry

# Установка
```commandline
poetry install
playwright install
```

# Запуск автотестов
```poetry run pytest```

# Параметры

`-H` или `--headless` - для запуска тестов в режиме без UI